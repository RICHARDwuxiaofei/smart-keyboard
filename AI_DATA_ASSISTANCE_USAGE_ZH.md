# AI 数据辅助功能使用说明

本文说明当前 APK 中已经实现的 AI 数据辅助功能如何配置、如何触发、如何测试，以及遇到问题时怎么排查。

当前重点先使用“手动模式”。OCR/截图模式仍保留入口，但暂时不是主要测试路径。

## 1. 安装 APK

推荐安装当前仓库根目录下的 arm64 调试包：

```text
D:\CODE\keybord\fcitx5-android\fcitx5-android-ai-arm64-debug.apk
```

安装到 BlueStacks 或真机后，进入系统输入法设置，把“小企鹅输入法（调试）”启用并切换为当前输入法。

如果安装失败，常见原因：

- 设备架构不是 arm64。可以改用 `app/build/outputs/apk/debug/` 下的 x86 / x86_64 / armeabi-v7a 包。
- 旧版调试包签名冲突。先卸载旧版“小企鹅输入法（调试）”，再安装新版。

## 2. 打开 AI 设置

有两个入口。

### 入口 A：系统设置页

打开“小企鹅输入法（调试）”设置页，在主列表里进入：

```text
AI 数据辅助
```

这里可以配置：

- 是否启用 AI 模式
- 触发模式：截图模式 / 手动模式
- 供应商
- 请求地址
- 模型名称
- API Key
- 前置提示词
- 功能介绍
- 本地 Skill 知识库导入
- DeepSeek API 测试

### 入口 B：键盘右上角三个点

在键盘界面右上角点三个点，会弹出菜单：

```text
AI 截图模式
AI 手动模式
关闭 AI 模式
AI 设置
状态区域
```

选择 `AI 手动模式` 后，AI 手动模式会立即启用。

## 3. DeepSeek API 推荐配置

当前代码已经支持 DeepSeek 的 OpenAI 兼容格式和 Anthropic 兼容格式。

推荐先使用 Anthropic 兼容格式：

```text
供应商：DeepSeek
请求地址：https://api.deepseek.com/anthropic
模型名称：deepseek-v4-flash
```

如果要使用更强模型：

```text
模型名称：deepseek-v4-pro
```

API Key 填你在 DeepSeek 控制台创建的 Key。

保存后，点：

```text
测试 DeepSeek API
```

如果测试通过，会弹出短提示，例如：

```text
API 测试通过：OK
```

如果测试失败，先检查：

- API Key 是否为空或复制错。
- 请求地址是否是 `https://api.deepseek.com/anthropic`。
- 模型是否写成 `deepseek-v4-flash` 或 `deepseek-v4-pro`。
- BlueStacks / 真机是否能联网。
- 当前网络是否允许访问 DeepSeek。

## 4. 手动模式完整操作流程

手动模式用于“不依赖 OCR，自己输入题目再发给 AI”。

### 第一步：切到 AI 手动模式

在键盘界面点右上角三个点，选择：

```text
AI 手动模式
```

菜单里出现 `AI 手动模式 ✓` 表示当前已选择。

### 第二步：打开键盘底部输入框

长按句号键：

```text
.
```

中文标点状态下显示为：

```text
。
```

两种都可以。长按后，键盘最下方会弹出一个小输入框，提示：

```text
输入题目，长按空格发送
```

### 第三步：在底部输入框里输入题目

底部输入框打开后，键盘上的普通按键会优先写进这个底部输入框，而不是写进浏览器/目标 App 的输入框。

可用操作：

- 字母键：输入文字
- 符号键：输入符号
- 空格点按：输入空格
- 退格：删除底部输入框里的最后一个字符
- 右侧 `×`：收起底部输入框，但保留已输入内容

示例：

```text
请根据这个页面内容，告诉我今天应该填写哪个日期字段
```

### 第四步：抓取当前应用输入框内容

如果你想让 AI 直接分析当前浏览器/目标 App 输入框里的内容，可以点逗号键：

```text
,
```

中文标点状态下显示为：

```text
，
```

在 AI 手动模式开启时，点逗号不会输入逗号，而是读取当前目标输入框内容，并填入底部手动输入框。

如果 AI 手动模式没有开启，逗号仍然是普通逗号。

### 第五步：发送给 AI

长按空格键。

长按空格后，会按以下流程执行：

```text
底部输入框内容
-> 本地 Skill 知识库优先匹配
-> 本地未命中则请求 DeepSeek
-> AI 结果写入剪贴板
-> AI 结果进入输入法候选栏第 0 项
```

注意：手动模式下，点按空格仍然只是普通空格；发送必须长按空格。

## 5. AI 结果在哪里看

当前结果会进入两个位置。

### 位置 A：系统剪贴板

AI 返回的答案会自动复制到系统剪贴板。

你可以在浏览器/目标 App 里长按输入框，使用系统粘贴。

### 位置 B：输入法剪贴板栏

项目原本有剪贴板历史功能。AI 结果写入系统剪贴板后，会触发输入法剪贴板监听，通常也会进入输入法自己的剪贴板历史。

如果键盘顶部出现剪贴板建议，点它可以上屏。

### 位置 C：候选栏第 0 项

AI 结果也会作为候选栏第一项出现。

点击它时，会走平滑逐字输入器，把文本逐字提交到当前输入框。这个适合目标系统不支持粘贴，或者一次性提交大段文本容易卡住的场景。

如果你只想用剪贴板，不想上屏，不要点击这个候选项即可。

## 6. 截图模式说明

截图模式入口仍然保留：

```text
键盘右上角三个点 -> AI 截图模式
```

截图模式下，长按空格会尝试：

```text
申请屏幕捕获权限
-> 截取单帧屏幕
-> 本地 ML Kit OCR
-> 本地 Skill 搜索
-> DeepSeek 兜底
```

当前阶段先不重点测试 OCR。如果出现 OCR 为空、权限弹窗异常、识别不准，可以先切回手动模式。

## 7. 本地 Skill 知识库怎么导入

进入：

```text
AI 数据辅助 -> 本地 Skill 知识库导入
```

支持 JSON 和简单文本。

### JSON 数组格式

```json
[
  {
    "keyword": "日期怎么填",
    "answer": "请填写当前业务日期，格式为 YYYY-MM-DD。"
  },
  {
    "keyword": "季度字段",
    "answer": "2026 年 7 月属于第三季度。"
  }
]
```

### JSON 对象格式

```json
{
  "skills": [
    {
      "keyword": "发票抬头",
      "answer": "发票抬头请填写企业全称。"
    }
  ]
}
```

### TXT 格式

每行一条，支持以下分隔符：

```text
日期怎么填=>请填写当前业务日期，格式为 YYYY-MM-DD。
季度字段|2026 年 7 月属于第三季度。
发票抬头:发票抬头请填写企业全称。
客户编号	客户编号请使用系统左上角显示的编号。
```

导入方式：

- `追加导入 Skill`：保留原有数据，新增或覆盖同 keyword 的内容。
- `替换本地 Skill 库`：清空旧数据，再导入当前文本。
- `清空本地 Skill 库`：删除本地 Skill 数据。

## 8. 前置提示词怎么写

默认提示词会把用户内容放进 `[OCR_RESULT]` 占位符。

推荐手动模式提示词示例：

```text
你是一个企业内网业务填写助手。请根据用户输入的问题或表单内容，给出可以直接复制粘贴到目标系统的最终答案。不要解释推理过程，不要输出 Markdown。用户内容如下：[OCR_RESULT]
```

如果你希望输出更短：

```text
请只输出最终应填写的字段值，不要解释。用户内容：[OCR_RESULT]
```

如果你希望输出操作步骤：

```text
请根据用户内容输出 1 到 3 条简短操作步骤，每条不超过 20 个字。用户内容：[OCR_RESULT]
```

## 9. 常见问题

### 长按句号没有弹出底部输入框

检查：

- 是否已经在三个点菜单里选择 `AI 手动模式`。
- 是否装的是最新 APK。
- 是否长按的是主键盘右下角的句号键。

### 点逗号没有抓到当前输入框内容

可能原因：

- 目标 App 不支持 `InputConnection.getExtractedText()`。
- 当前输入框没有焦点。
- BlueStacks / 浏览器地址栏处于特殊浮层状态。

代码已做 fallback，会尝试读取光标前后文本。如果目标 App 完全不暴露文本，抓取结果可能为空。

### 长按空格没有返回结果

检查：

- AI 模式是否开启。
- 触发模式是否是 `手动模式`。
- API Key 是否配置。
- 是否点过 `测试 DeepSeek API`。
- 本地网络是否能访问 DeepSeek。
- 底部输入框是否有内容。

### 返回结果没看到

结果不会直接弹大窗口提示。请看：

- 系统剪贴板是否有新内容。
- 输入法顶部剪贴板建议是否出现。
- 候选栏第一个候选词是否变成 AI 结果。

### 普通逗号不能用了

这是手动模式下的设计：逗号被临时用于“抓取当前输入框内容”。

需要普通逗号时：

- 关闭 AI 模式；或
- 切到截图模式；或
- 在底部输入框里用符号键/标点输入逗号。

### 不想让 AI 结果直接上屏

不要点击候选栏第 0 项即可。

当前 AI 结果会先写剪贴板。候选栏点击只是额外的平滑上屏入口。

## 10. 快速测试脚本

推荐第一次这样测：

1. 安装最新 APK。
2. 打开输入法设置页。
3. 进入 `AI 数据辅助`。
4. 请求地址填：

```text
https://api.deepseek.com/anthropic
```

5. 模型填：

```text
deepseek-v4-flash
```

6. 填 API Key。
7. 点 `保存 AI 配置`。
8. 点 `测试 DeepSeek API`。
9. 打开浏览器地址栏或任意文本框。
10. 键盘右上角三个点选择 `AI 手动模式`。
11. 长按句号打开底部输入框。
12. 输入：

```text
请只回复 OK
```

13. 长按空格发送。
14. 查看系统剪贴板或候选栏第 0 项。

## 11. 当前实现边界

- 没有修改 C++ 底层。
- 手动模式不依赖 OCR。
- 截图模式仍保留，但当前阶段不作为主要验证目标。
- AI 结果会写入系统剪贴板；是否进入输入法剪贴板历史，取决于系统剪贴板监听是否开启。
- 如果目标 App 禁止读取输入框内容，逗号抓取可能为空。
- 如果目标 App 禁止粘贴，可以点击候选栏第 0 项走逐字上屏。
